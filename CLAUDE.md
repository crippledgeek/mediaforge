# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository

**bash** — A workspace for POSIX shell / Bash projects.

### Projects

| Directory | Description |
|---|---|
| `mediaforge/` | POSIX shell build system that compiles FFmpeg 8.0.1 from source with ~50 modular dependency recipes |

Each project has its own `CLAUDE.md` with project-specific instructions.
