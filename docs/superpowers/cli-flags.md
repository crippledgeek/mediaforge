# mediaforge CLI flags reference

Generated listing of every recipe and the CLI flags that affect it.

## Group selectors

| Flag | Allowed values | Default | Notes |
|---|---|---|---|
| `--tls=BACKEND` | `openssl`, `gnutls`, `mbedtls`, `libressl`, `none` | `gnutls` | mbedtls and libressl recipes are static-only. |
| `--aac=IMPL` | `fdk_aac`, `native` | `native` | `fdk_aac` requires `--enable-nonfree`. |
| `--h264=IMPL` | `x264`, `openh264` | `x264` (with `--enable-gpl`) | Both encoders require MPEG-LA patent licensing for redistribution. See "Patent caveat" below. |
| `--h265=IMPL` | `x265`, `kvazaar` | `x265` (with `--enable-gpl`) | x265 is GPL; Kvazaar is LGPL. Same patent caveat as H.264. |
| `--av1-enc=IMPL` | `svtav1`, `rav1e`, `av1` | `svtav1` | `av1` = libaom (reference encoder; slow). |

## Generic toggles

- `--enable=PKG` — force-enable a recipe that defaults to off. Does **not** override `--enable-gpl` / `--enable-nonfree` / platform guards.
- `--disable=PKG` — disable a recipe by name. Repeatable; commas accepted.
- `--list-pkgs` — print every recipe.
- `--menu` — interactive selector (whiptail with POSIX fallback).
- `--clean-choices` — delete the persistent choice file.

## Precedence ladder

When multiple sources set a group choice, highest precedence wins:

1. Explicit CLI flag (`--tls=`, `--aac=`, etc.)
2. `--menu` selections (only when `--menu` is passed)
3. Smart-prompt answers from the current run
4. `$PREFIX/.mediaforge-choices` (stored choices from the previous run)
5. Active profile's `*_DEFAULT` value
6. Built-in conservative default

`--dry-run` skips reading and writing the stored choices file so test runs are hermetic.

## Patent caveat (H.264 / H.265)

Both `x264`/`openh264` and `x265`/`kvazaar` are patent-encumbered when *built from source*. OpenH264's BSD-3 license covers the source code only; Cisco's royalty-free distribution applies exclusively to their pre-built binary download, which mediaforge does not consume. End users distributing binaries built with these encoders remain liable for MPEG-LA royalties unless they qualify for an exemption. Choosing between `x264` and `openh264` (or `x265` and `kvazaar`) is a software-license trade-off, not a patent trade-off.

## Recipe inventory

The table below is generated by `./mediaforge.sh build --list-pkgs` — regenerate at release time with:

```sh
./mediaforge.sh build --list-pkgs \
  | grep -v '^\[mediaforge\]' \
  | awk -F'\t' '{ printf "| `%s` | %s | `%s` | %s |\n", $1, $2, $3, $4 }'
```

| Recipe | Category | Mutex group | Flags |
|---|---|---|---|
| `giflib` | tools | `-` | - |
| `pkg-config` | tools | `-` | - |
| `yasm` | tools | `-` | - |
| `nasm` | tools | `-` | - |
| `zlib` | tools | `-` | - |
| `m4` | tools | `-` | - |
| `autoconf` | tools | `-` | - |
| `automake` | tools | `-` | - |
| `libtool` | tools | `-` | - |
| `gettext` | other | `-` | nonfree |
| `openssl` | crypto | `tls` | - |
| `mbedtls` | crypto | `tls` | - |
| `libressl` | crypto | `tls` | - |
| `gmp` | crypto | `-` | - |
| `nettle` | crypto | `-` | - |
| `gnutls` | crypto | `tls` | - |
| `cmake` | tools | `-` | - |
| `dav1d` | video | `-` | - |
| `svtav1` | video | `av1-enc` | - |
| `rav1e` | video | `av1-enc` | - |
| `x264` | video | `h264` | gpl |
| `x265` | video | `h265` | gpl |
| `libvpx` | video | `-` | - |
| `xvidcore` | video | `-` | gpl |
| `vid_stab` | video | `-` | gpl |
| `av1` | video | `av1-enc` | - |
| `zimg` | video | `-` | - |
| `kvazaar` | video | `h265` | - |
| `openh264` | video | `h264` | - |
| `lv2` | audio | `-` | - |
| `opencore` | audio | `-` | - |
| `lame` | audio | `-` | - |
| `opus` | audio | `-` | - |
| `libogg` | audio | `-` | - |
| `libvorbis` | audio | `-` | - |
| `libtheora` | audio | `-` | - |
| `fdk_aac` | audio | `aac` | nonfree |
| `soxr` | audio | `-` | - |
| `speex` | audio | `-` | - |
| `twolame` | audio | `-` | - |
| `gsm` | audio | `-` | - |
| `libilbc` | audio | `-` | - |
| `vo_amrwbenc` | audio | `-` | nonfree |
| `libtiff` | image | `-` | - |
| `libpng` | image | `-` | - |
| `libjxl` | image | `-` | - |
| `libwebp` | image | `-` | - |
| `openjpeg` | image | `-` | - |
| `libsdl` | other | `-` | - |
| `freetype2` | other | `-` | - |
| `libxml2` | other | `-` | - |
| `fribidi` | other | `-` | - |
| `harfbuzz` | other | `-` | - |
| `freetype2-harfbuzz` | other | `-` | - |
| `fontconfig` | other | `-` | - |
| `libass` | other | `-` | - |
| `libbluray` | other | `-` | - |
| `librtmp` | other | `-` | - |
| `vapoursynth` | other | `-` | - |
| `srt` | other | `-` | nonfree |
| `zvbi` | other | `-` | nonfree |
| `libzmq` | other | `-` | - |
| `rubberband` | other | `-` | gpl |
| `libmysofa` | other | `-` | - |
| `bs2b` | other | `-` | - |
| `chromaprint` | other | `-` | - |
| `frei0r` | other | `-` | gpl |
| `ladspa` | other | `-` | - |
| `librist` | other | `-` | - |
| `libcaca` | other | `-` | - |
| `codec2` | other | `-` | - |
| `flite` | other | `-` | - |
| `libgme` | other | `-` | - |
| `libopenmpt` | other | `-` | - |
| `libshine` | other | `-` | - |
| `libsnappy` | other | `-` | - |
| `vulkan-headers` | hwaccel | `-` | - |
| `glslang` | hwaccel | `-` | - |
| `nv-codec` | hwaccel | `-` | - |
| `vaapi` | hwaccel | `-` | - |
| `amf` | hwaccel | `-` | - |
| `opencl` | hwaccel | `-` | - |
