# Agent / AI tool files (per-developer, not source of truth)
CLAUDE.md
CLAUDE.local.md
AGENTS.md
GEMINI.md
.claude/
.cursor/
.cursorrules
.cursorignore
.windsurf/
.windsurfrules
.aider*
.continue/
.superpowers/
graphify-out/

# Build artifacts
docs/
packages/
workspace/

# Deliberately-broken fixtures planted by tests/pre-push-hook.sh. Trapped on
# every ordinary exit; ignored here for the kill a trap cannot catch, so a leaked
# one cannot be adopted as a test by tests/oracle-baseline.sh.
.githooks/.pre-push-fixture-*
